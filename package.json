{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "pnpm -F application dev",
    "dev:admin": "cd package/admin && npm run dev",
    "dev:server": "cd package/server && npm run dev",
    "build": "pnpm -r application build",
    "build:admin": "cd package/admin && npm run build",
    "install:all": "pnpm install",
    "install:admin": "cd package/admin && npm install"
  },
  "devDependencies": {
  }
}
